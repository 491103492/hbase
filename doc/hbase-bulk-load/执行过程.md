##  1. 运行 gen.py 生成数据 xxx.csv 


### 2. 需要在HBase中创建一个目标表
create 'TRANSFER_RECORD','C1'
### 3.将这个转账记录的CSV文件上传到HDFS中
rz将数据上传到linux操作

hdfs dfs -mkdir -p /hbase/bank_record/input
hdfs dfs -put bank_record.csv /hbase/bank_recoord/input